window.onload = calculate;
function calculate(){
	/*
		let breedte;
		let lengte;
		let hoogte;
		do{
			breedte = Number(prompt("Geef de breedte:"));
		} while (breedte <= 0 || !breedte);
		do{
		lengte = Number(prompt("Geef de lengte:"));
		} while (lengte <= 0 || !lengte);
		do{
			hoogte = Number(prompt("Geef de hoogte:"));
		} while (hoogte <= 0 || !hoogte);
		document.write("<h1>Een tegel met een breedte van " + breedte + " cm, een hoogte van " + hoogte + " cm en een lengte van " + lengte + " cm,<br>heeft een volume van " + (lengte * breedte * hoogte) + " cm³.</h1>");
	*/

}